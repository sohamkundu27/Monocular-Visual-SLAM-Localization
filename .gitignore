# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/

# Virtual environments
.venv/
venv/
env/

# Testing / tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
coverage.xml

# Notebooks
.ipynb_checkpoints/

# Editors / OS
.vscode/
.idea/
*.swp
.DS_Store

# Datasets: never commit KITTI (or any raw sensor data) into the repo.
# Anchored to the repo root so src/monocular_slam/datasets/ stays tracked.
/data/
/datasets/
/kitti/
/sequences/
/poses/
*.bin
*.pcd

# Run outputs: regenerated by scripts/run_slam.py, not version controlled.
# Curated result images that the README references live in docs/results/.
/outputs/
