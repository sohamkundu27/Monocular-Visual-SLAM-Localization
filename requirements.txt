# Runtime dependencies. See pyproject.toml for the authoritative specification.
#
# gtsam 4.2.2 wheels are compiled against the NumPy 1.x ABI, which is why the
# stack is pinned below NumPy 2.0.
numpy>=1.24,<2.0
opencv-python>=4.8
scipy>=1.10
matplotlib>=3.7
PyYAML>=6.0
gtsam>=4.2,<4.3

# Development / testing
pytest>=7.4
ruff>=0.4
